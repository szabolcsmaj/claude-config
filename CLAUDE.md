@~/.claude/rules/docker/docker-compose.md
@~/.claude/rules/python/python-conventions.md

# Git
- NEVER run `git push` (or any push variant: `--force`, `-f`, `--force-with-lease`, pushing tags, pushing to a specific remote/branch) unless I explicitly tell you to push in that turn. A prior approval does not carry over to later turns.
